@schema_version 1

# WP-011: example exemption record fixture, for reference and for
# the integration test in `test_naudit_baseline.c`.
#
# The rule_id below is the content hash of the canonical
# `n00b.s2_1.null` rule's BNF fragment (computed from
# audit-rules.bnf). The region_fingerprint is the hash of the
# canonical bytes of the NULL keyword in expression position (a
# whitespace-normalized form of "NULL" — the matched span on a
# typical fixture_null.c line).
#
# The integration test computes both values at runtime and
# regenerates the file before the assertion, so the values here
# are illustrative (no test asserts against them directly).

@exemption sample_null_exemption_0001
@version 1
@rule_id 0000000000000000000000000000beef
@rule_name n00b.s2_1.null
@file_path test/fixtures/naudit/fixture_null.c
@locator_line 5
@locator_col 14
@locator_end_line 5
@locator_end_col 18
@region_fingerprint 0000000000000000000000000000cafe
@rationale Example exemption only — not a real waiver.
@signer_id placeholder
@approved_at 2026-05-28T00:00:00Z

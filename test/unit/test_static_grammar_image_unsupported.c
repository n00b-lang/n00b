#include "n00b.h"

int
main(void)
{
    n00b_init_simple(0, nullptr);
    n00b_print(r"[SKIP] static grammar-image object emission is unsupported on this platform; using runtime BNF fallback");
    return 0;
}

#pragma once

/**
 * @file parser_fwd.h
 * @brief Forward declarations and shared parse tree type.
 *
 * Re-exports parse_tree.h (parse tree types and the generic
 * `n00b_parse_fn_t` abstraction).
 */

#include "slay/parse_tree.h"

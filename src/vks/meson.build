# Virtual KV store — a parameterized container (typed dict + pluggable
# durability backend). Phase 2 adds the snapshot-to-vfs backend.

n00b_vks_src = files(
    'types.c',
    'backend.c',
    'backend_snapshot.c',
    'vks.c',
)

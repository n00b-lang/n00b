# Cross-cutting utilities used by the core.

subdir('simd')

n00b_util_src = (
    files(
        'assert.c',
        'base64.c',
        'errno_str.c',
        'path.c',
    )
    + n00b_util_simd_src
)

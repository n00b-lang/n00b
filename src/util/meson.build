# Cross-cutting utilities used by the core.

subdir('simd')

n00b_util_src = (
    files(
        'assert.c',
        'base64.c',
        'der_encode.c',
        'errno_str.c',
        'path.c',
        'pkcs7.c',
    )
    + n00b_util_simd_src
)

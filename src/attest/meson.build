# Sources for the n00b attest module: in-toto Statement v1
# construction, DSSE envelope encode/decode, and (in later WPs)
# signer backends, OCI 1.1 registry referrers, and the verifier
# surface. Phase 1 ships only the module-init stub; Phase 2 adds
# the Statement / DSSE bodies.

n00b_attest_src = files(
    'n00b_attest_module.c',
    'statement.c',
    'dsse.c',
    'json_util.c',
)

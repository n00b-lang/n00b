# Text processing — Unicode algorithms, string formatting / styling,
# and the regex engine. Each leaf subdirectory has its own meson.build.

subdir('unicode')
subdir('strings')
subdir('regex')

n00b_text_src = (
    n00b_text_unicode_src +
    n00b_text_strings_src +
    n00b_text_regex_src
)

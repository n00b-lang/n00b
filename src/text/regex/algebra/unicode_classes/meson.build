# Algebra unicode_classes port — Phase 5. Mirrors
# ~/resharp-c/src/algebra/unicode_classes/. These are algebra-side regex
# character class builders (UTF-8 byte-range trees for \d / \w / \s and full
# Unicode variants), NOT vendored unicode tables.

n00b_text_regex_algebra_unicode_classes_src = files(
    'classes.c',
    'mod.c',
)

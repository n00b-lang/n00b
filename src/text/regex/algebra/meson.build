# Algebra port — Phase 5. Mirrors ~/resharp-c/src/algebra/.

subdir('unicode_classes')

n00b_text_regex_algebra_src = (
    n00b_text_regex_algebra_unicode_classes_src
    + files(
        'algebra.c',
        'nulls.c',
        'solver.c',
    )
)

# QR code generation — data encoding, Reed-Solomon ECC interleaving,
# matrix placement/masking, and terminal rendering. Built on the
# reusable crypto/reed_solomon + crypto/gf256 primitives.

n00b_text_qrcode_src = files(
    'qrcode.c',
)
